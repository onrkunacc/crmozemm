@{
    ViewData["Title"] = "Erişim Reddedildi";
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif; /* Modern ve zarif yazı tipi */
            background-color: #f4f7f6; /* Daha yumuşak bir arka plan rengi */
            margin: 0;
            height: 100vh; /* Yüksekliği tam ekran yapar */
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .card {
            border: none;
            border-radius: 15px; /* Yumuşatılmış köşeler */
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15); /* Daha belirgin ama yumuşak bir gölge */
            background-color: #ffffff; /* Kartın arka plan rengi */
            padding: 30px;
            max-width: 450px;
            width: 100%;
            text-align: center;
        }

            .card img {
                width: 150px;
                margin-bottom: 20px;
            }

        .card-body h1 {
            font-size: 2.2rem;
            font-weight: 600;
            color: #e74c3c; /* Hafifçe parlak kırmızı renk */
            margin-bottom: 15px;
        }

        .card-body p {
            font-size: 1rem;
            color: #34495e; /* Koyu ama yumuşak metin rengi */
            line-height: 1.6;
        }

        .card-body .btn {
            display: inline-block;
            padding: 10px 20px;
            border-radius: 8px;
            text-decoration: none;
            font-size: 14px;
            font-weight: bold;
            transition: background-color 0.3s ease, color 0.3s ease;
            background-color: #f0f0f0;
            color: #333;
            border: 1px solid #ddd;
        }

            .card-body .btn:hover {
                background-color: #e0e0e0;
            }
    </style>
</head>
<body>
    <div class="card">
        <img src="~/Modernize-1.0.0/src/assets/images/logos/logo.jpg" alt="Logo">
        <div class="card-body">
            <h1 class="display-4">🚫 Erişim Reddedildi!</h1>
            <hr />
            <p>
                Üzgünüz, bu sayfaya erişim izniniz bulunmamaktadır.<br>
                Lütfen farklı bir sayfa deneyin veya ana sayfaya geri dönün.
            </p>
            <a href="~/Home/Index" class="btn">Ana Sayfaya Dön</a>
        </div>
    </div>
</body>
</html>
